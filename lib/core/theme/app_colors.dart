import 'package:flutter/material.dart';

/// Central color tokens in case custom colors are required outside [ColorScheme].
abstract class AppColors {
  static const seed = Color(0xFF0066CC);
  static const onSeed = Colors.white;
}
